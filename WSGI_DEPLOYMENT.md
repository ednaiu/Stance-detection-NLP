# WSGI Deployment Guide for Stance Detection

Этот документ описывает развертывание приложения как WSGI (без Docker) на shared hosting от reg.ru.

## 📋 Предварительные условия

- ✅ SSH доступ к серверу (u3089870@31.31.198.9)
- ✅ Python 3.6+ установлен
- ✅ pip и venv доступны
- ✅ Git установлен

## 🚀 Быстрое развертывание

### Для Linux/Mac/WSL:

```bash
# Перейти в корень проекта
cd c:\Users\ednay\PycharmProjects\stance_detection

# Запустить скрипт развертывания
bash deploy_wsgi.sh u3089870 31.31.198.9
```

### Для Windows (PowerShell):

```powershell
# Если у вас есть Git Bash или WSL
bash deploy_wsgi.sh u3089870 31.31.198.9

# Или вручную выполнить команды ниже
```

## 📝 Ручное развертывание

### Шаг 1: Подключиться к серверу

```bash
ssh u3089870@31.31.198.9
```

### Шаг 2: Создать директорию приложения

```bash
mkdir -p /var/www/u3089870/data/stance-detection
cd /var/www/u3089870/data/stance-detection
```

### Шаг 3: Клонировать репозиторий

```bash
git clone https://github.com/ednaiu/Stance-detection.git .
# или
git pull origin main
```

### Шаг 4: Создать виртуальное окружение

```bash
python3 -m venv venv
source venv/bin/activate

# Обновить pip
pip install --upgrade pip setuptools wheel

# Установить зависимости
pip install -r StanceClassifier/requirements.txt
```

**⚠️ Примечание**: На первой установке это займет 5-10 минут, так как будут компилироваться PyTorch и transformers.

### Шаг 5: Создать стартовый скрипт

```bash
cat > run.sh << 'EOF'
#!/bin/bash
cd /var/www/u3089870/data/stance-detection
source venv/bin/activate
export PYTHONUNBUFFERED=1
export MODEL_PATH=models/sentence_embedding_baseline
gunicorn -w 2 -b 0.0.0.0:5000 --timeout 60 --access-logfile - wsgi:app
EOF

chmod +x run.sh
```

### Шаг 6: Протестировать локально

```bash
# Запустить приложение
./run.sh

# Вывод должен быть примерно:
# [2026-02-08 12:34:56 +0000] [12345] [INFO] Starting gunicorn 21.2.0
# [2026-02-08 12:34:56 +0000] [12345] [INFO] Listening at: http://0.0.0.0:5000
```

### Шаг 7: Проверить в браузере (на другом терминале)

```bash
# На локальной машине
curl http://31.31.198.9:5000/health

# Или откройте в браузере
# http://31.31.198.9:5000/health
```

## 🔄 Запуск в фоне (постоянное выполнение)

### Вариант 1: Используя screen (простейший)

```bash
# Запустить в screen сессии
screen -S stance
./run.sh

# Нажать Ctrl+A, затем D для выхода (процесс продолжит работать)

# Позже можно подключиться к сессии
screen -r stance

# И выйти из screen
# Ctrl+A, затем :quit
```

### Вариант 2: Используя nohup

```bash
nohup ./run.sh > stance.log 2>&1 &

# Проверить логи
tail -f stance.log

# Остановить процесс
pkill -f "gunicorn"
```

### Вариант 3: Используя systemd (если у вас есть sudo)

Обратитесь в поддержку reg.ru с просьбой создать systemd сервис:

```ini
[Unit]
Description=Stance Detection Flask API
After=network.target

[Service]
Type=notify
User=u3089870
WorkingDirectory=/var/www/u3089870/data/stance-detection
ExecStart=/var/www/u3089870/data/stance-detection/venv/bin/gunicorn \
    -w 2 -b 0.0.0.0:5000 --timeout 60 wsgi:app
Restart=on-failure
RestartSec=5s

[Install]
WantedBy=multi-user.target
```

## 📊 Мониторинг приложения

### Проверить статус

```bash
# Смотреть процессы
ps aux | grep gunicorn

# Смотреть потребление ресурсов
top -p $(pgrep -f gunicorn | tr '\n' ',')

# Проверить логи (если используется nohup)
tail -f stance.log
```

### Проверить API

```bash
# Health check
curl http://31.31.198.9:5000/health

# Информация о приложении
curl http://31.31.198.9:5000/

# Классификация одного текста
curl -X POST http://31.31.198.9:5000/classify \
  -H "Content-Type: application/json" \
  -d '{"text": "I like this", "target": "topic"}'

# Пакетная классификация
curl -X POST http://31.31.198.9:5000/classify_batch \
  -H "Content-Type: application/json" \
  -d '[{"text": "I like this", "target": "topic"}]'
```

## 🆘 Решение проблем

### Проблема: "Port 5000 already in use"

```bash
# Найти процесс на порту 5000
lsof -i :5000

# Убить процесс
kill -9 <PID>

# Или использовать другой порт
gunicorn -b 0.0.0.0:8080 wsgi:app
```

### Проблема: "Module not found"

```bash
# Убедиться что venv активирован
source venv/bin/activate

# Переустановить зависимости
pip install -r StanceClassifier/requirements.txt --force-reinstall
```

### Проблема: "timeout"

Увеличить timeout в run.sh:
```bash
gunicorn -w 2 -b 0.0.0.0:5000 --timeout 300 wsgi:app
```

### Проблема: Нет доступа к моделям

Убедиться что модели скачаны:
```bash
# Проверить наличие
ls -la models/sentence_embedding_baseline/

# Или скачать
python3 << 'EOF'
from huggingface_hub import snapshot_download
snapshot_download(
    'strombergnlp/rumoureval_2019',
    repo_type='model',
    local_dir='models/sentence_embedding_baseline'
)
EOF
```

## 📞 Поддержка reg.ru

Если нужна помощь хостинга:

1. **Создать systemd сервис** - обратитесь в поддержку
2. **Открыть порт 5000** - может быть закрыт firewall
3. **Увеличить лимиты ресурсов** - если приложение медленное
4. **SSH доступ** - если потеряли доступ

Обычно ответ в течение 24 часов.

## 🔗 Полезные ссылки

- [Gunicorn документация](https://docs.gunicorn.org/)
- [Flask документация](https://flask.palletsprojects.com/)
- [reg.ru техподдержка](https://www.reg.ru/support/)
- [GitHub репозиторий](https://github.com/ednaiu/Stance-detection)

## 📈 Масштабирование

Если приложение будет получать много запросов:

1. **Увеличить рабочих процессов**: `-w 4` вместо `-w 2`
2. **Использовать Nginx** как reverse proxy
3. **Перейти на VPS** для лучших ресурсов

```bash
# Для более мощного сервера
gunicorn -w 4 -b 0.0.0.0:5000 --timeout 60 wsgi:app
```

---

**Последнее обновление**: 2026-02-08
**Версия**: 2.5.1
