# Быстрая настройка безопасного хранения паролей

## 1️⃣ Создайте файл .env

```powershell
# Скопируйте шаблон
Copy-Item .env.example .env

# Откройте и введите реальный пароль
notepad .env
```

В файле `.env` замените:
```
REMOTE_HOST=31.31.198.9
REMOTE_USER=root
REMOTE_PASSWORD=ваш_реальный_пароль_от_regru
```

## 2️⃣ Готово! Теперь используйте:

### Вариант A: Автоматическая загрузка

```powershell
# Полный pipeline (автоматически загрузит .env)
.\commit_and_deploy.ps1 "My changes"
```

### Вариант B: Загрузить переменные вручную

```powershell
# Загрузить переменные
. .\load_env.ps1

# Деплой
.\deploy.ps1
```

## ✅ Проверка безопасности

```powershell
# Убедитесь, что .env НЕ в git
git status
# .env НЕ должен быть в списке изменений

# Проверьте .gitignore
Get-Content .gitignore | Select-String "\.env"
# Должно показать: .env
```

## ⚠️ ВАЖНО

- ✅ `.env` уже в `.gitignore` - пароль НЕ попадет в git
- ✅ Можно безопасно создавать `.env` локально
- ❌ Никогда не удаляйте `.env` из `.gitignore`
- ❌ Никогда не коммитьте `.env` в git

## 📖 Подробнее

Полная документация: [SECURITY_CREDENTIALS.md](SECURITY_CREDENTIALS.md)

## Где взять пароль?

1. **Email от reg.ru** - после заказа VPS
2. **Личный кабинет** - https://www.reg.ru → Серверы → ваш сервер
3. **Сбросить пароль** - в панели управления сервером
