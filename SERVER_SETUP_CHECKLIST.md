# Чек-лист настройки сервера 31.31.198.9

## 📋 Информация для подключения
- **IP-адрес**: 31.31.198.9
- **Пользователь**: u3089870
- **Пароль**: N4LkM8x63YWKfo2w
- **Панель управления**: https://server232.hosting.reg.ru:1500/
- **Пароль панели**: N4LkM8x63YWKfo2w

---

## 1️⃣ Проверка SSH доступа

### ✓ Что проверить:
- SSH сервис запущен (порт 22)
- Логин и пароль работают

### Команда для теста (выполнить в терминале хоста):
```bash
# Подключиться по SSH и проверить базовые команды
ssh u3089870@31.31.198.9 'whoami'
# Ввести пароль: N4LkM8x63YWKfo2w
```

---

## 2️⃣ Проверка Docker

### ✓ Что проверить:
- Docker установлен
- Docker запущен и работает
- Docker доступен пользователю u3089870

### Команды для проверки:
```bash
ssh u3089870@31.31.198.9 'docker --version'
ssh u3089870@31.31.198.9 'docker ps'
ssh u3089870@31.31.198.9 'docker run hello-world'
```

### Если Docker не установлен:
```bash
# Подключиться и установить Docker
ssh u3089870@31.31.198.9
sudo apt-get update
sudo apt-get install -y docker.io
sudo systemctl start docker
sudo systemctl enable docker

# Добавить пользователя в группу docker (чтобы не вводить sudo)
sudo usermod -aG docker u3089870
newgrp docker
```

---

## 3️⃣ Создание необходимых директорий

### ✓ Структура директорий на сервере:
```
/data/
└── stance-detection/
    ├── data/          (данные для классификации)
    ├── models/        (обученные модели)
    └── docker/        (отправляется автоматически)
```

### Команды для создания:
```bash
ssh u3089870@31.31.198.9 << 'EOF'
# Создать директории
mkdir -p /data/stance-detection/data
mkdir -p /data/stance-detection/models

# Проверить права доступа
ls -la /data/

# Убедиться что у пользователя есть права
chmod 755 /data/stance-detection
chmod 755 /data/stance-detection/data
chmod 755 /data/stance-detection/models

# Проверить что файлы видны
touch /data/stance-detection/.access_check
ls -la /data/stance-detection/
EOF
```

---

## 4️⃣ Проверка Firewall и портов

### ✓ Открытые порты:
- **22** (SSH) - уже открыт ✓
- **5000** (Flask API) - нужно открыть
- **443** (HTTPS опционально)
- **80** (HTTP опционально)

### Проверить статус портов:
```bash
ssh u3089870@31.31.198.9 << 'EOF'
# Проверить какие порты открыты
sudo ss -tlnp 2>/dev/null | grep LISTEN

# Или через netstat
sudo netstat -tlnp 2>/dev/null | grep LISTEN
EOF
```

### Если нужно открыть порт 5000:
```bash
ssh u3089870@31.31.198.9 << 'EOF'
# Для UFW firewall
sudo ufw allow 5000/tcp
sudo ufw status

# Или для iptables
sudo iptables -A INPUT -p tcp --dport 5000 -j ACCEPT
sudo iptables-save
EOF
```

---

## 5️⃣ Проверка дискового пространства

```bash
ssh u3089870@31.31.198.9 << 'EOF'
# Проверить место на диске
df -h

# Проверить место в /data
du -sh /data/

# Требуется минимум:
# - 5 GB для Docker образа
# - 2 GB для моделей
# - 2 GB для данных
EOF
```

---

## 6️⃣ Проверка переменных окружения

```bash
ssh u3089870@31.31.198.9 << 'EOF'
# Убедиться что можно установить переменные
echo $HOME
export DOCKER_HOST
export MODEL_PATH
echo "OK"
EOF
```

---

## 7️⃣ Полная проверка готовности

### Выполните этот блок для полной диагностики:
```bash
ssh u3089870@31.31.198.9 << 'EOF'
echo "=== SERVER READINESS CHECK ==="
echo ""
echo "1. User info:"
whoami
echo ""
echo "2. Docker version:"
docker --version 2>&1 || echo "Docker NOT installed"
echo ""
echo "3. Docker running:"
docker ps 2>&1 || echo "Docker NOT running"
echo ""
echo "4. Data directories:"
ls -la /data/stance-detection/ 2>&1 || echo "Directory NOT created"
echo ""
echo "5. Disk space:"
df -h / | tail -1
echo ""
echo "6. Available ports (SSH should be here):"
sudo ss -tlnp 2>/dev/null | grep LISTEN | head -5 || echo "Cannot check ports"
echo ""
echo "=== END CHECK ==="
EOF
```

---

## 📝 Чек-лист выполнения

- [ ] SSH доступ работает
- [ ] Docker установлен
- [ ] Docker запущен
- [ ] Директории `/data/stance-detection/data` созданы
- [ ] Директории `/data/stance-detection/models` созданы
- [ ] Порт 5000 открыт в firewall
- [ ] Дискового пространства достаточно (>5GB)
- [ ] Пользователь u3089870 может запускать Docker

---

## 🚀 После выполнения чек-листа

Когда всё готово, запустите deploy:

```powershell
cd c:\Users\ednay\PycharmProjects\stance_detection
.\deploy.ps1
```

Или запустите полный pipeline:
```powershell
.\commit_and_deploy.ps1 "Server ready - deploying application"
```

---

## 🆘 Частые проблемы

### Проблема: "Permission denied" при создании /data
**Решение**: Используйте `sudo` или создайте директорию в `/home/u3089870`

### Проблема: Docker требует sudo
**Решение**: 
```bash
sudo usermod -aG docker u3089870
# Затем перезагрузиться или выполнить: newgrp docker
```

### Проблема: Не хватает места на диске
**Решение**: Используйте команду `df -h` для проверки и очистите ненужные файлы

### Проблема: Порт 5000 занят
**Решение**: Измените порт в docker-compose.yml или закройте приложение занимающее порт

---

## 📞 Технические данные

Все компоненты используют стандартные порты:
- **API Flask**: http://31.31.198.9:5000
- **Health check**: http://31.31.198.9:5000/health
- **Classification**: POST http://31.31.198.9:5000/classify
